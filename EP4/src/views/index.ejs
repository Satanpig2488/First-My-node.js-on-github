<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- Routing to product page by query string-->
    <h1>Routing to product page by query string</h1>
    <form method="get" action="/products_query">
        ใส่รหัสสินค้า<input type="text" name="product_id" value="ใส่รหัสสินค้าที่นี่">
        <input type="submit">
    </form>
    <hr>
    <!-- End of Routi ng to product page-->
    <!-- Routing to product page by parameter-->
    <h1>Routing to product page by parameter</h1>
    <% Object.values(products).forEach(function(products){ %>
        ....<a href="/products_param/<%= products.id %>"> <%= products.name %> </a>
    <%}) %>
    <hr>
    <!-- End of Routi ng to product page by parameter-->
    <h1>Hello <%=name%></h1>
    <!-- Get and show data -->
    <h1>Hello EJS Engine</h1>
    <%= name %>.<%= lname %>.<%= age %>
    <%- address %>
    <hr>
    <!-- Condition If -->
    IF of EJS
    <% if(age>=18){ %> 
        <p><h1>Pass</h1></p> 
    <% } else {%>
        <p><h1>Not Pass</h1></p> 
    <% } %>
    <hr>
    <!-- End Condition If -->
    <!-- For Loop -->
     For Loop<br>
    <% for(i=1;i<=10;i++) {%>
        แสดงผลรอบที่ <%= i %><br>
    <%}%>
    <hr>
    <!-- End For Loop -->
    <!-- Get Array -->
    Array<br>
    แสดงผลครั้งเดียว .... <%= car  %><br>
    เลือกแสดงผลบาง index..... <%= car[0] %>*<%= car[2] %><br>
    มีรถทั้งหมด <%=  car.length %> คัน<br>
    <ol>
    <% for(i=0;i<car.length;i++) {%>
        <li>แสดงผลรถ <%= car[i] %> จาก for loop<br>
    <%}%>
    </ol>
    <ol>
        <% for(index in car) {%>
            <li>แสดงผลรถ <%= car[index] %> จาก for in loop<br>
        <%}%>
    </ol>
    <ul>
        <%
            car.map((i)=>{ %>
                <li>แสดงผลรถ <%= i %> จาก .map loop</li>
            <%})
        %>
    </ul>
    <hr>
    <!-- End Get Array -->
    <!-- Get Object Array -->
    Object inside Array<br>
    ระบุแบบง่าย <%= products  %><br>
    วน Loop แสดง Object ด้วย forEach<br>
    <% Object.values(acc).forEach(function($acc){%>
        <%= $acc.accname %>..*..<%= $acc.accbrand %>...*....<%= $acc.accmodel %>...*...<%= $acc.acccolor %><br>
    <%}) %>
    <!-- End Get and show data -->
    <hr>
</body>
</html>